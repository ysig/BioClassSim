#PROJECT IS NOW IN: STUDY VERSION

#A library to read and classify biological data, based on similarity measures.

#Bio I/O Dependencies:

# BioPython
sudo pip install biopython

# SVM/classifier Dependencies:
sudo pip install scipy
sudo pip install sklearn

# Parallel Execution
sudo pip install joblib
sudo pip install multiprocessing

# Dill for pickling instance methods:
sudo pip install dill
